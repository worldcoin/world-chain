name: ELF

on:
  merge_group:
  pull_request:
    branches: [main]
    paths:
      - 'proofs/succinct/programs/**'
      - 'proofs/core/**'
      - 'proofs/kona-client/**'
      - 'proofs/succinct/utils/client/**'
      - 'proofs/succinct/utils/ethereum-client/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify-elfs:
    runs-on: arc-public-8xlarge-amd64-runner
    name: verify ELFs
    steps:
      - uses: actions/checkout@v6
      - uses: taiki-e/install-action@just
      - name: Install SP1 toolchain
        run: |
          curl -L https://sp1.succinct.xyz | bash
          ~/.sp1/bin/sp1up --version v6.1.0
          echo "$HOME/.sp1/bin" >> $GITHUB_PATH
      - name: Build ELFs
        run: just build-proof-elfs
      - name: Verify ELFs unchanged
        run: |
          if [[ -n $(git diff proofs/succinct/elf/) ]]; then
            echo "ELF files changed after rebuild — commit the updated ELFs"
            git diff proofs/succinct/elf/
            exit 1
          fi
