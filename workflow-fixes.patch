diff --git a/.github/workflows/docker-proof.yml b/.github/workflows/docker-proof.yml
index 53a7c43..68d2717 100644
--- a/.github/workflows/docker-proof.yml
+++ b/.github/workflows/docker-proof.yml
@@ -327,4 +327,4 @@ jobs:
           platforms: linux/amd64
           tags: |
             ${{ env.REGISTRY }}/${{ github.repository }}-nitro-worker-enclave-eif:${{ inputs.image_tag || 'nightly' }}
-            ${{ env.REGISTRY }}/${{ github.repository }}-nitro-worker-enclave-eif:sha-${{ github.sha }}
\ No newline at end of file
+            ${{ env.REGISTRY }}/${{ github.repository }}-nitro-worker-enclave-eif:sha-${{ github.sha }}
diff --git a/.github/workflows/release-proof.yml b/.github/workflows/release-proof.yml
index 9929ea1..b8313fb 100644
--- a/.github/workflows/release-proof.yml
+++ b/.github/workflows/release-proof.yml
@@ -333,8 +333,10 @@ jobs:
   # pods can pull it via an init container (AWS Nitro Enclaves K8s device plugin pattern).
   build-nitro-worker-enclave-eif-image:
     name: build nitro-worker-enclave EIF carrier image
-    needs: [build-nitro-worker-enclave-image]
+    needs: [build-nitro-worker-enclave-image, extract-version]
     runs-on: arc-public-8xlarge-amd64-runner
+    outputs:
+      digest: ${{ steps.push_eif_image.outputs.digest }}
     steps:
       - name: Check Out Repo
         uses: actions/checkout@v6
@@ -351,6 +353,7 @@ jobs:
       - name: Set up Docker Buildx
         uses: docker/setup-buildx-action@v3
       - name: Build and push EIF carrier image
+        id: push_eif_image
         uses: docker/build-push-action@v6
         with:
           context: target/eif
@@ -434,7 +437,7 @@ jobs:
         with:
           name: nitro-enclave
           path: |
-            target/eif/world-chain-nitro-enclave.eif
+            target/eif/world-chain-nitro-worker-enclave.eif
             target/eif/pcrs.json
           if-no-files-found: error
 
@@ -448,6 +451,7 @@ jobs:
       - merge-nitro-prover-image
       - build-binaries
       - build-eif
+      - build-nitro-worker-enclave-eif-image
     runs-on: ubuntu-latest
     permissions:
       contents: write
@@ -455,6 +459,7 @@ jobs:
       VERSION: ${{ needs.extract-version.outputs.VERSION }}
       SP1_PROVER_IMAGE_DIGEST: ${{ needs.merge-sp1-prover-image.outputs.digest }}
       NITRO_PROVER_IMAGE_DIGEST: ${{ needs.merge-nitro-prover-image.outputs.digest }}
+      EIF_IMAGE_DIGEST: ${{ needs.build-nitro-worker-enclave-eif-image.outputs.digest }}
     steps:
       - uses: actions/checkout@v6
         with:
@@ -468,7 +473,7 @@ jobs:
           mkdir -p assets
           cp artifacts/vkeys/vkeys.json assets/
           cp artifacts/nitro-enclave/pcrs.json assets/
-          cp artifacts/nitro-enclave/world-chain-nitro-enclave.eif assets/
+          cp artifacts/nitro-enclave/world-chain-nitro-worker-enclave.eif assets/
           cp artifacts/world-chain-prover-*/*.tar.gz* assets/
       - name: Build manifest
         run: |
@@ -479,6 +484,8 @@ jobs:
             --arg sp1_image_digest "$SP1_PROVER_IMAGE_DIGEST" \
             --arg nitro_image "${REGISTRY}/${PROOF_IMAGE_NAME}-nitro:${VERSION}" \
             --arg nitro_image_digest "$NITRO_PROVER_IMAGE_DIGEST" \
+            --arg eif_image "${REGISTRY}/${GITHUB_REPOSITORY}-nitro-worker-enclave-eif:${VERSION}" \
+            --arg eif_image_digest "$EIF_IMAGE_DIGEST" \
             --slurpfile vkeys assets/vkeys.json \
             --slurpfile pcrs assets/pcrs.json \
             '{
@@ -488,7 +495,8 @@ jobs:
               nitro_enclave: { pcrs: $pcrs[0] },
               images: {
                 sp1_prover: { name: $sp1_image, digest: $sp1_image_digest },
-                nitro_prover: { name: $nitro_image, digest: $nitro_image_digest }
+                nitro_prover: { name: $nitro_image, digest: $nitro_image_digest },
+                eif_launcher: { name: $eif_image, digest: $eif_image_digest }
               }
             }' > assets/manifest.json
           cat assets/manifest.json
@@ -552,10 +560,11 @@ jobs:
           | `manifest.json` | Binds git SHA, vkeys, PCRs, and image digests for this release |
           | `vkeys.json` | SP1 range vkey commitment + aggregation vkey |
           | `pcrs.json` | Nitro enclave PCR0/PCR1/PCR2 |
-          | `world-chain-nitro-enclave.eif` | Enclave image (measurements in `pcrs.json`) |
+          | `world-chain-nitro-worker-enclave.eif` | Enclave image (measurements in `pcrs.json`) |
           | `world-chain-prover-*.tar.gz` | `world-chain-prover-sp1` and `world-chain-prover-nitro` binaries, signed with PGP key `C75F BC64 E9D4 8E89 FB60 418B 8949 B352 D042 2E74` |
           | SP1 Docker | `ghcr.io/${{ env.PROOF_IMAGE_NAME }}-sp1:${{ env.VERSION }}` |
           | Nitro Docker | `ghcr.io/${{ env.PROOF_IMAGE_NAME }}-nitro:${{ env.VERSION }}` |
+          | EIF Launcher | `ghcr.io/${{ github.repository }}-nitro-worker-enclave-eif:${{ env.VERSION }}` |
           ENDBODY
           )
           gh release create --draft -t "proof/${VERSION}" -F - "proof/${VERSION}" assets/* <<< "$body"
